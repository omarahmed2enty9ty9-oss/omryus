node_modules/
dist/
*.zip
.DS_Store
mock-store/_harness.js

# Rendered brag videos and their vendored audio — build artifacts, not source.
brag-output/
brag-output-*/

# Secrets. The repo is public — a committed token is scraped within minutes.
.env
.env.*
!.env.example

# Wrangler cache
.wrangler/
